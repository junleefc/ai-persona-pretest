# GUIDE.md · 클로드가 따르는 진행 안내

이 문서는 사람이 아니라 Claude(Claude Code)가 읽고 그대로 실행하는 안내다.
사용자는 개발을 모른다. 터미널을 쓸 줄 모른다. 질문은 한국어로, 짧게 한다.

목표: 사용자가 만든 랜딩페이지를 가상 한국인 7명에게 보여주고,
"신청 버튼(CTA)을 누를 것인가, 안 누르면 어디서 멈췄나, 뭐라고 바뀌면 누르나"를 받아
사용자가 페이지의 어느 문장을 고쳐야 하는지 한 가지로 알려준다.

---

## 0. 원칙

- 페르소나 파일에 적힌 사실만 쓴다. 파일에 없는 사실(자녀 수, 소득, 앱 사용 이력 등)은 지어내지 않는다. 모르는 건 그냥 답하지 않고 넘어간다. "프로필로는 알 수 없음"이라는 말은 한 사람당 최대 한 번만 쓴다. 답마다 반복하면 읽기 어렵다.
- 다만 프로필의 성격, 직업, 생활 패턴에서 자연스럽게 추론되는 반응은 쓴다. 예: 새벽 5시에 출근하는 제빵사는 아침에 준비물을 챙기기 어렵다. 이건 지어낸 게 아니라 프로필에서 나온 것이다.
- 긍정 편향 금지. 가상 고객은 사용자 편이 아니다. 답은 그 사람 프로필에서만 끌어낸다. 다 쓰고 나서 7명 전원이 "누른다"로 나왔다면 후하게 본 것이니 다시 본다. 이건 **사후 점검이지 맞춰야 할 목표가 아니다.** 분포를 미리 정해 놓고 답을 거기에 맞추지 않는다.
- 결과는 전부 가설이다. 리포트 끝에 반드시 "실제 사람 3명에게 확인하라"와 실제 인터뷰 질문 3개를 넣는다.
- 판정, 점수, GO/KILL 같은 말은 쓰지 않는다. "7명 중 N명이 누른다"와 "가장 많이 걸린 구간 1개"만 말한다.
- 출력 문장은 짧게. "혁신적인", "최고의" 같은 말은 쓰지 않는다. 이모지와 em-dash(—)는 쓰지 않는다.

---

## 1. 입력 파일 읽기

현재 폴더(사용자가 Claude Code에서 연 폴더)에서 아래 세 파일을 찾아 읽는다.

| 파일 | 찾는 법 | 없으면 |
|---|---|---|
| 기획서 | `*.md` 중 `PRD.md`, `REPORT*.md`, `GUIDE.md`, `README.md`를 제외한 파일. 여러 개면 가장 최근 것. | 페이지가 있으면 페이지 텍스트에서 여섯 가지를 추정해 진행한다. 표 제목에 "(기획서 없음, 페이지에서 추정)"이라고 적는다. 페이지도 없으면 멈춘다. |
| PRD.md | 그 이름 그대로 | 기획서만으로 진행한다. 있으면 페이지 구조 파악에 쓴다. |
| index.html | 그 이름 그대로. 없으면 `*.html` 중 하나. | 사용자가 프롬프트에 사이트 주소를 줬으면 `curl -sL 주소 -o index.html`로 받아 쓴다. 주소도 없으면 "랜딩페이지 index.html이나 사이트 주소가 필요해요. 페이지를 먼저 만들거나 배포한 주소를 알려 주세요."라고 말하고 멈춘다. |

**사용자가 사이트 주소를 준 경우** (예: "내 사이트는 https://navicon.co.kr 이야"): 폴더에 index.html이 있어도 주소를 우선한다. 배포된 것이 최신이기 때문이다. 받은 파일은 `index.html`로 저장하고, 리포트의 페이지 파일 이름 자리에는 주소를 적는다.

**받은 HTML이 클 때** (Next.js, React 등으로 만든 사이트는 수백 KB): 전체를 읽지 말고 python으로 `<script>`, `<style>`, `<svg>`를 제거한 뒤 태그를 벗겨 보이는 텍스트만 뽑는다. 그 텍스트를 위에서 아래 순서로 구간을 나눈다. 구간 제목이 없으면 h1, h2, 버튼 문구를 기준으로 나눈다.

기획서에서 아래 여섯 가지를 뽑아 표로 사용자에게 먼저 보여준다. 기획서 양식이 달라도(1회차 린캔버스, 2회차 한 장 기획서, 자유 형식) 의미로 찾는다.

| 항목 | 기획서에서 찾는 것 |
|---|---|
| 사업 한 줄 | 누구에게 무엇을 판다 |
| 누구 | 나이, 하는 일, 상황. 페르소나 선별의 기준 |
| 불편 | 그 사람이 언제 무엇을 못 하고 있나 |
| 지금 방법과 차이 | 지금은 어떻게 하는데, 내 것은 무엇이 다른가 |
| 누가 얼마 | 가격과 비교 대상 |
| 무료로 줄 것 + 신청 항목 | 페이지의 CTA가 신청받는 것, 폼에서 받는 항목 |

신청 버튼이 구글폼, 탈리 같은 외부 폼으로 연결되면 그 주소도 `curl -sL`로 받아 폼 항목(이름, 이메일 등)을 뽑아 "무료로 줄 것 + 신청 항목" 칸에 적는다.

비어 있는 항목([확인 필요] 포함)은 페이지 텍스트에서 채울 수 있으면 채우고 "(페이지에서 추정)"이라고 표시한다. 페이지에도 없으면 "기획서에 없음"으로 두고 진행한다. 사용자에게 되묻지 않는다.

index.html은 실제로 열어 텍스트를 위에서 아래 순서로 뽑아 둔다. 페이지는 보통 네 구간이다.

1. 첫 화면: 불편 한 문장
2. 차이: 지금 방법과 다른 점
3. 무료 제안: 무료로 주는 것
4. 신청: 버튼과 폼 항목

구간이 이와 다르면 실제 페이지 순서대로 구간을 정하되, 4개 이내로 묶는다. 묶을 때 **구간 크기가 비슷하도록** 나눈다. 한 구간에 페이지 절반이 들어가면 그 구간이 최빈이 되기 쉬워 결과가 왜곡된다.

---

## 2. 페르소나 데이터 준비

100만 명 전체에서 찾는다. 방법은 두 단계다. 색인 파일(15MB, 100만 명의 나이·성별·지역·직업·혼인·가구·상황 태그)로 내 컴퓨터에서 조건 검색을 하고, 고른 후보의 전체 프로필만 HuggingFace에서 받아온다. API 키는 없다.

현재 폴더에 `index.tsv.gz`와 `find_personas.py`가 없으면 받는다.

```bash
curl -fL -o index.tsv.gz https://raw.githubusercontent.com/junleefc/ai-persona-pretest/main/index.tsv.gz
curl -fL -o find_personas.py https://raw.githubusercontent.com/junleefc/ai-persona-pretest/main/scripts/find_personas.py
```

이름은 `persona` 텍스트 첫머리에 "OOO 씨"로 나온다. 그 이름을 그대로 쓴다. 새 이름을 짓지 않는다.

출처: NVIDIA Nemotron-Personas-Korea (CC BY 4.0). 리포트 맨 아래에 이 한 줄을 넣는다.

---

## 3. 7명 고르기

기획서의 "누구"에서 조건을 뽑아 `find_personas.py`를 돌린다. 쓸 수 있는 조건: 나이 범위, 성별, 지역, 직업 키워드, 혼인, 가구 형태, 상황 태그.

**먼저 정한다. 이 타깃은 직업인가, 상황인가.**

| 타깃 유형 | 예 | 쓰는 조건 |
|---|---|---|
| 직업 | 레미콘 영업사원, 방송작가, 초등교사, 임업인 | `--occupation` |
| 상황 | 수험생, 취업준비생, 퇴사 준비 직장인, 초보 부모, 은퇴 예정자 | `--tag` |
| 둘 다 | 육아하는 직장인 | `--occupation` + `--tag` |

상황 타깃을 직업으로 찾으면 안 된다. 예를 들어 "회계사 시험 준비 중인 대학생"을 `--occupation 무직`으로 찾으면 시험 준비와 무관한 무직자가 뽑히고, 그 사람들은 당연히 신청 버튼을 안 누른다. 페이지 문제가 아니라 사람을 잘못 데려온 것이다. 이때는 `--age 20-29 --tag 수험생`을 쓴다.

쓸 수 있는 상황 태그 열여섯 개다. 프로필 본문에서 미리 뽑아 둔 것이다.

`수험생` `취업준비` `창업준비` `퇴사은퇴` `육아` `학부모` `돌봄간병` `건강관리` `반려동물` `재테크` `귀농귀촌` `프리랜서` `1인가구` `외국어유학` `콘텐츠창작` `결혼준비`

태그로도 안 잡히는 아주 구체적인 상황(예: 공인회계사 시험)은 태그로 넓게 뽑은 뒤, 받아 온 후보의 `persona`와 `career_goals_and_ambitions` 본문에서 python으로 한 번 더 거른다. 그래도 없으면 가장 가까운 상황으로 진행하고, 무엇을 무엇으로 대체했는지 `sampling`의 `notes`에 적는다.

```bash
# 직업 타깃
python3 find_personas.py --age 30-55 --sex 남자 --occupation 자재,레미콘,시멘트,콘크리트 --occupation-all 영업 --region 경기,서울,인천 --n 24

# 상황 타깃
python3 find_personas.py --age 20-29 --tag 수험생 --n 24
```

- `--occupation`은 쉼표로 여러 개를 OR로 건다. 직업은 2,120종으로 세분돼 있다(예: 건축자재 영업원, 초등학교 교사, 온라인 쇼핑 판매원, 임업 종사자). 각 값은 직업명에 포함되는 글자라서 띄어쓰기 없는 짧은 단어로 넣는다(`기계장비`처럼). 업종 타깃이면 동의어를 함께 건다. 레미콘, 시멘트처럼 직업명에 없는 말은 매칭이 안 되니 `자재`, `건설` 같은 넓은 말을 같이 넣는다.
- `--occupation-all`은 반드시 들어가야 하는 글자(AND). 예: `--occupation-all 영업`.
- `--family 자녀`는 가구 형태에 자녀가 있는 사람. `--marital 배우자있음`.
- 화면에 "100만 명 중 조건 매칭 N명"이 나오고, 그중 무작위 24명의 전체 프로필이 `candidates.json`에 저장된다. 1분쯤 걸린다. 몇 명 실패해도 10명 이상 받았으면 그대로 진행한다.
- 스크립트가 "대체 파일에서 찾았습니다"라고 하면 접속이 막힌 것이다. 그대로 진행하되 선별 메모에 적는다.
- `candidates.json` 형식: `{"matched_total": N, "fetched": 40, "candidates": [ {...전체 프로필...} ]}`.

데이터의 한계를 알고 조건을 세운다.
- **자녀 나이와 학년은 프로필에 거의 없다.** "초등 2학년 자녀"는 `--family 자녀 --age 30-45 --tag 육아,학부모`로 대체한다. 학년은 모르는 채로 둔다.
- **배우자가 일하는지는 알 수 없다.** "맞벌이"는 "일하는 부모"로 읽는다. 아빠 페르소나도 뽑는다.
- **과거 사건 기록이 없다.** "언제 겪었나"는 물을 수 없다. 생활 패턴에서 그 불편이 생길 상황이 있는지를 본다.
- 색인에는 프로필 본문이 없다. 태그로도 안 잡히는 세부 조건은 후보를 받은 뒤 `persona`, `professional_persona`, `family_persona`, `career_goals_and_ambitions` 텍스트에서 python으로 한 번 더 거른다.

**매칭이 20명 미만이면** 조건을 하나씩 푼다. 푸는 순서: 지역 → 나이 범위 → 혼인·가구 → 직업 동의어 추가. **타깃 직업은 끝까지 지킨다.** 직업이 다른 사람을 타깃 5명에 넣지 않는다. 그런 사람은 인접 세그먼트 2명으로만 쓴다.

**조건을 다 풀어도 인접 세그먼트 2명을 못 찾으면**(타깃 직업이 100만 명 안에서도 수십 명뿐인 경우) 가장 가까운 직업으로 대체한다. 예를 들어 방송작가를 못 찾으면 미디어 콘텐츠 창작자로. 대체했다는 사실과 원래 무엇을 찾으려 했는지를 `sampling`의 `notes`에 반드시 적는다. 직업 이름이 달라도 프로필 본문에 그 일을 한다고 적혀 있으면 같은 직업으로 본다. 어떤 조건을 어떻게 풀었는지 "선별 메모" 두 줄로 남긴다. 이 메모는 채팅과 REPORT.md 둘 다에 들어간다.

**find_personas.py가 프로필을 못 받으면**(HuggingFace 접속이 막히면) 아래 대체 파일을 같은 폴더에 받아 두고 스크립트를 다시 돌린다. 스크립트가 알아서 이 파일에서 같은 조건으로 찾는다. 13,798명이고 2,120개 직업이 모두 들어 있어 드문 직업도 몇 명은 있다. 선별 메모에 "대체 파일 사용"이라고 적는다.

```bash
curl -fL -o personas.jsonl https://raw.githubusercontent.com/junleefc/ai-persona-pretest/main/personas.jsonl \
  || (curl -fL -o personas.jsonl.gz https://cdn.jsdelivr.net/gh/junleefc/ai-persona-pretest@main/personas.jsonl.gz && gunzip -f personas.jsonl.gz)
```

받아 온 후보에서 7명을 고른다. 다섯 명과 두 명의 역할이 다르다.

**핵심 타깃 5명** (`kind: target`). 기획서의 "누구"에 맞는 사람이다. 직업·지역·가족 상황이 서로 겹치지 않게 고른다. 이 5명의 답이 "내 페이지가 내 고객에게 통하는가"를 말해 준다.

**인접 세그먼트 2명** (`kind: near`). 조건에서 **한 가지만** 벗어난 고객군이다. 한 명은 나이가 10살쯤 위나 아래, 한 명은 직업이 다르지만 같은 불편을 겪을 법한 사람으로 고른다. `find_personas.py`를 조건을 바꿔 두 번 더 돌린다.

```bash
python3 find_personas.py --age 56-65 (나머지 조건 같게) --n 5 --out near_age.json
python3 find_personas.py --occupation 기계장비 (나머지 조건 같게) --n 5 --out near_occ.json
```

이 2명은 페이지를 평가하려고 넣는 게 아니다. **타깃 범위를 넓혀도 되는지 확인하는 비교군**이다. 벗어난 사람도 "필요하다"고 하면 타깃을 넓혀도 되고, "나랑 상관없다"고 하면 지금 범위가 맞다. 리포트에서는 이 답으로 "타깃을 넓혀도 되나, 좁혀야 하나" 한 줄을 쓴다.

고른 7명을 아래 표로 사용자에게 보여준 뒤 인터뷰로 넘어간다. 사용자 승인을 기다리지 않는다.

| 번호 | 이름 | 나이·성별 | 지역 | 직업 | 가족 | 핵심 타깃 / 인접 세그먼트 | 고른 이유 한 줄 |

---

## 4. 인터뷰 (7명 각각)

각 사람의 persona, professional_persona, family_persona, skills, hobbies, career_goals를 전부 읽고 그 사람이 되어 1인칭으로 답한다. 말투는 그 사람의 나이와 직업에 맞게. 답은 짧게, 각 질문에 2~3문장.

**Q1 불편.** 기획서의 불편이 생길 상황이 당신 일상에 있나. 있다면 어떤 장면인가. 없다면 왜 없나.

**Q2 지금 방법.** 그 불편을 지금은 어떻게 넘기고 있나. 그게 얼마나 귀찮은가. (프로필로 알 수 없으면 그렇게 쓴다.)

**Q3 차이.** 기획서의 "내 것은 이게 다르다"가 당신에게 의미 있나. 아니면 지금 방법이랑 별 차이 없나.

**Q4 가격.** 기획서의 가격을 낼 것인가. 이미 돈 내고 쓰는 비슷한 게 있나.

**Q5 페이지.** index.html 텍스트를 위에서 아래로 읽었다고 치고:
- 신청 버튼을 누르나: **누른다 / 안 누른다 / 모르겠다** 중 하나만.
- 안 누르거나 모르겠으면, 어느 구간에서 멈췄나: **첫 화면 / 차이 / 무료 제안 / 신청 항목** 중 하나만. (신청 항목이 걸림돌이면 어떤 항목이 부담인지도.) 누른 사람은 멈춘 곳이 **없음**이다.
- 뭐라고 바뀌면 누르나: 본인 말투로 한 문장. 누른다고 한 사람도 "이건 좀 걸렸다" 한 가지를 쓴다.

주의: Q1에서 불편이 없다고 한 사람은 Q5에서 "안 누른다"가 자연스럽다. 억지로 누르게 하지 않는다.

---

## 5. 종합

**먼저 패널을 점검한다.** 핵심 타깃 5명 중 3명 이상이 "나는 이 서비스가 겨냥한 사람이 아니다"라는 이유로 첫 화면에서 멈췄다면, 그건 페이지가 나쁜 게 아니라 **타깃과 같은 사람을 못 찾은 것**이다. 이때는 `report.json`에 `panel_title`과 `panel_note`를 채운다. 리포트 맨 위에 경고가 뜬다.

- `panel_title` 예: "이 결과는 페이지 문제가 아닙니다"
- `panel_note` 예: "100만 명 안에 공인회계사 수험생이 없어, 다른 시험을 준비하는 수험생 5명에게 물었습니다. 이 사람들이 안 누른 이유는 '내 시험이 아니어서'입니다. 페이지 문장을 고칠 근거로 쓰지 마세요. 대신 아래 질문 3개를 실제 수험생에게 물어보세요."

이 경우 3번 "바꿀 문장"은 억지로 만들지 말고, 타깃을 다시 정의하거나 실제 사람을 만나라는 내용으로 쓴다. 클릭 수는 그대로 두되 참고 수치다. 패널이 타깃과 맞으면 `panel_note`는 `null`로 둔다.

7명 결과를 모아 아래 순서로 정리한다. 이 순서가 리포트 상단이다.

1. **결과 한 줄.** "7명 중 N명이 누른다. M명은 안 누르고, K명은 모르겠다."
2. **가장 많이 걸린 구간 1개.** 계산 대상은 아래 순서로 정한다.

   - **핵심 타깃 5명만 센다.** 인접 세그먼트 2명은 넣지 않는다. 내 고객이 아닌 사람이 멈춘 지점은 페이지를 고칠 근거가 되지 않는다. 이 2명의 답은 5번 타깃 범위 판단에만 쓴다.
   - 그중 **신청하지 않은 사람과 망설인 사람**(verdict가 no 또는 maybe)이 멈춘 구간을 센다. 신청한 사람의 "걸린 것"은 여기 넣지 않는다. 전환을 막은 장벽과 있으면 좋을 개선점은 무게가 다르기 때문이다.
   - 그중 **Q1에서 불편 자체가 없다고 한 사람은 뺀다.** 문장을 고쳐도 신청하지 않을 사람이다. "불편은 있는데 급하지 않다"는 뺄 대상이 아니다.
   - 남은 사람의 최빈 구간을 고른다. **동률이면** 신청한 사람이 "걸렸다"고 말한 구간을 가산점으로 써서 동률을 깬다. 그래도 동률이면 페이지에서 더 위에 있는 구간을 고른다. 앞에서 막히면 뒤는 읽히지 않기 때문이다.
   - **`stop_basis`에 근거 인원을 적는다.** 모집단과 그중 몇 명인지를 함께 쓴다. 예: "신청하지 않았거나 망설인 핵심 타깃 3명 중 2명".
   - **최빈 구간을 지목한 사람이 2명 이하면** "2명이 지목한 결과라 근거가 얇다"를 `stop_why`에 덧붙인다. 모집단 크기가 아니라 그 구간을 지목한 사람 수 기준이다.

3. **바꿀 문장 1개.** 이건 제안이지 정답이 아니다. 쓸지 말지는 사용자가 정한다. 그 구간에서 index.html에 실제로 있는 문장 하나를 인용하고(현재), 인터뷰 답을 근거로 바꾼 문장(제안)을 쓴다. 문장 하나는 태그 하나(h1, h2, p 중 하나)의 텍스트 전체다. 제안 문장은 기획서에 있는 말로 쓰고, 없는 약속을 지어내지 않는다.
4. **신청 항목.** 신청 항목이 걸림돌로 나온 사람이 있으면 "폼 질문을 O개로 줄이거나 OO 항목을 빼라"고 쓴다. 없으면 이 줄은 생략.
5. **인접 세그먼트가 말해 주는 것.** 타깃 밖 2명의 반응으로 "타깃을 넓혀도 되나 / 좁혀야 하나" 한 줄.
6. **이건 가설이다.** "가상 고객 7명의 답은 통계 프로필로 만든 추정이다. 실제 사람 3명에게 아래 세 질문을 물어보고 확인하라." 그리고 실제 인터뷰 질문 3개. 질문은 이번 결과에서 가장 불확실했던 것을 겨냥한다.

---

## 6. 출력

두 파일을 현재 폴더에 저장한다.

**REPORT-(오늘 날짜).md** : 파일 이름에 오늘 날짜를 붙인다. 예: `REPORT-2026-09-14.md`. 여러 번 돌려도 지난 결과가 남아 비교할 수 있다. 내용은 이 순서로 쓴다.
1. 제목, 날짜, 읽은 파일 이름
2. 기획서에서 뽑은 여섯 가지 표 (1절)
3. 페이지 구간 (1절에서 뽑은 index.html 텍스트, 구간별로)
4. 종합 (5절의 여섯 항목)
5. 7명 표 + 선별 메모 (3절)
6. 7명 인터뷰 전문 (4절)
7. 출처 한 줄

사용자가 다음에 "리포트 결론을 반영해서 고쳐줘"라고 하면 이 폴더에서 가장 최근 REPORT md 파일을 읽는다. 종합 3번의 "현재 문장"은 index.html에 있는 그대로 정확히 인용한다.

**REPORT-(오늘 날짜).html** : 손으로 HTML을 만들지 않는다. `report.json`을 쓰고 스크립트로 만든다.

1. 템플릿과 스크립트를 받는다.

```bash
curl -fL -o report-template.html https://raw.githubusercontent.com/junleefc/ai-persona-pretest/main/templates/report.html
curl -fL -o fill_report.py https://raw.githubusercontent.com/junleefc/ai-persona-pretest/main/scripts/fill_report.py
```

2. 현재 폴더에 `report.json`을 쓴다. 형식은 `fill_report.py` 맨 위 주석에 있다. 요약하면:
   - `project_name, date, brief_file, page_file`
   - `run_info`: 지금 쓰고 있는 모델 이름과 **실제로** 걸린 시간. 예: "Claude Opus 5로 6분 걸림". 시작 시각을 재지 않았으면 시간은 빼고 모델 이름만 적는다. 짐작해서 쓰지 않는다
   - `stop_section`(첫 화면 / 차이 / 무료 제안 / 신청 항목), `stop_basis`(예: "신청하지 않았거나 망설인 핵심 타깃 3명이 읽다가 멈춘 지점"), `stop_why`, `current_sentence`(index.html 원문 그대로), `proposed_sentence`
   - `stop_counts`: 네 구간별 인원. 예: `{"첫 화면": 1, "차이": 2, "무료 제안": 0, "신청 항목": 1}`. 2번에서 계산에 쓴 사람만 센다. 리포트에 막대로 그려진다
   - `form_note`(신청 항목 지적. 없으면 `null`), `near_insight`
   - `personas` 7명: `name, age, sex, district, occupation, kind(target|near), verdict(yes|no|maybe), stop(첫 화면|차이|무료 제안|신청 항목|없음), a1~a5, fix`
     - `a1~a5`는 다섯 질문의 답 전체다. 리포트에서 접혀 있다가 펼치면 다섯 개가 다 나오므로 하나도 비우지 않는다
     - `fix`는 그 사람이 남긴 한마디다. 접기 밖에 크게 보이는 문장이라 가장 중요한 말을 넣는다
   - `real_questions` 3개
   - `panel_title`, `panel_note`: 위 패널 점검에 걸렸을 때만. 아니면 `null`
   - `sampling`: 이 7명을 어떻게 뽑았는지. 리포트에 깔때기(100만 명 → 조건 매칭 N명 → 프로필 수신 N명 → 7명)와 조건 표로 그려진다. 반드시 채운다.
     - `matched_total`, `fetched`: find_personas.py 출력의 숫자 그대로
     - `match_line`: 검색 조건을 한 줄로 (예: "30~55세 남성, 서울·경기·인천, 직업에 자재 또는 건설 + 영업")
     - `fetch_line`: "N명 중 무작위 24명 요청, M명 수신"
     - `conditions`: 표 3줄. 타깃 조건(기획서의 누구 → 검색 조건 → 결과), 인접 세그먼트 1 (연령 축), 인접 세그먼트 2 (직업 축)
     - `notes`: 데이터 한계 1~3줄 (예: "직업명에 레미콘은 없어 건축자재 영업원으로 대체")
     - `candidates`: candidates.json과 인접 세그먼트 검색 결과의 후보 전원. 이름·나이·성별·지역·직업·가구. 7명에 든 사람은 `picked: true`
   - 누른다/모르겠다/안 누른다 수는 스크립트가 personas에서 센다. 따로 쓰지 않는다.

3. 실행한다.

```bash
python3 fill_report.py report.json report-template.html REPORT-2026-09-14.html
```

"saved"가 나오면 된 것이다. "채워지지 않은 자리"가 나오면 report.json에서 그 키를 채우고 다시 실행한다. 끝나면 `report-template.html`, `fill_report.py`, `find_personas.py`, `near_age.json`, `near_occ.json`은 지운다. `report.json`, `candidates.json`, `index.tsv.gz`는 둔다(다시 돌릴 때 쓴다).

저장이 끝나면 만든 HTML을 브라우저로 연다(macOS `open REPORT-2026-09-14.html`, Windows `start REPORT-2026-09-14.html`). 그리고 사용자에게 이렇게 마무리한다.

> 7명 중 N명이 누릅니다. 가장 많이 걸린 곳은 [구간]입니다.
> 제안을 쓸지는 직접 정하시면 됩니다. 그대로 쓰시려면 "리포트가 제안한 문장으로 바꿔줘"라고 하세요.
