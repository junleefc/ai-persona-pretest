# GUIDE.md · 클로드가 따르는 진행 안내

이 문서는 사람이 아니라 Claude(Claude Code)가 읽고 그대로 실행하는 안내다.
사용자는 개발을 모른다. 터미널을 쓸 줄 모른다. 질문은 한국어로, 짧게 한다.

목표: 사용자가 만든 랜딩페이지를 가상 한국인 7명에게 보여주고,
"신청 버튼(CTA)을 누를 것인가, 안 누르면 어디서 멈췄나, 뭐라고 바뀌면 누르나"를 받아
사용자가 페이지의 어느 문장을 고쳐야 하는지 한 가지로 알려준다.

---

## 0. 원칙

- 페르소나 파일에 적힌 사실만 쓴다. 파일에 없는 사실(자녀 수, 소득, 앱 사용 이력 등)은 지어내지 않는다. 모르는 건 그냥 답하지 않고 넘어간다. "프로필로는 알 수 없음"이라는 말은 한 사람당 최대 한 번만 쓴다. 답마다 반복하면 읽기 어렵다.
- 다만 프로필의 성격, 직업, 생활 패턴에서 자연스럽게 추론되는 반응은 쓴다. 예: 새벽 5시에 출근하는 제빵사는 아침에 준비물을 챙기기 어렵다. 이건 지어낸 게 아니라 프로필에서 나온 것이다.
- 긍정 편향 금지. 가상 고객은 사용자 편이 아니다. 7명 전원이 "누른다"면 잘못 돌린 것이다. 보통 7명 중 2~4명이 안 누르거나 망설인다.
- 결과는 전부 가설이다. 리포트 끝에 반드시 "실제 사람 3명에게 확인하라"와 실제 인터뷰 질문 3개를 넣는다.
- 판정, 점수, GO/KILL 같은 말은 쓰지 않는다. "7명 중 N명이 누른다"와 "가장 많이 걸린 구간 1개"만 말한다.
- 출력 문장은 짧게. "혁신적인", "최고의" 같은 말은 쓰지 않는다. 이모지와 em-dash(—)는 쓰지 않는다.

---

## 1. 입력 파일 읽기

현재 폴더(사용자가 Claude Code에서 연 폴더)에서 아래 세 파일을 찾아 읽는다.

| 파일 | 찾는 법 | 없으면 |
|---|---|---|
| 기획서 | `*.md` 중 `PRD.md`, `REPORT*.md`, `GUIDE.md`, `README.md`를 제외한 파일. 여러 개면 가장 최근 것. | "기획서 md 파일이 이 폴더에 없어요. 지난 워크시트에서 '마크다운으로 저장'한 파일을 이 폴더에 넣어 주세요."라고 말하고 멈춘다. |
| PRD.md | 그 이름 그대로 | 기획서만으로 진행한다. 있으면 페이지 구조 파악에 쓴다. |
| index.html | 그 이름 그대로. 없으면 `*.html` 중 하나. | "랜딩페이지 index.html이 없어요. 페이지를 먼저 만든 뒤 다시 해 주세요."라고 말하고 멈춘다. |

기획서에서 아래 여섯 가지를 뽑아 표로 사용자에게 먼저 보여준다. 기획서 양식이 달라도(1회차 린캔버스, 2회차 한 장 기획서, 자유 형식) 의미로 찾는다.

| 항목 | 기획서에서 찾는 것 |
|---|---|
| 사업 한 줄 | 누구에게 무엇을 판다 |
| 누구 | 나이, 하는 일, 상황. 페르소나 선별의 기준 |
| 불편 | 그 사람이 언제 무엇을 못 하고 있나 |
| 지금 방법과 차이 | 지금은 어떻게 하는데, 내 것은 무엇이 다른가 |
| 누가 얼마 | 가격과 비교 대상 |
| 무료로 줄 것 + 신청 항목 | 페이지의 CTA가 신청받는 것, 폼에서 받는 항목 |

비어 있는 항목은 "기획서에 없음"으로 두고 진행한다. 사용자에게 되묻지 않는다.

index.html은 실제로 열어 텍스트를 위에서 아래 순서로 뽑아 둔다. 페이지는 보통 네 구간이다.

1. 첫 화면: 불편 한 문장
2. 차이: 지금 방법과 다른 점
3. 무료 제안: 무료로 주는 것
4. 신청: 버튼과 폼 항목

구간이 이와 다르면 실제 페이지 순서대로 구간을 정하되, 4개 이내로 묶는다.

---

## 2. 페르소나 파일 준비

현재 폴더에 `personas.jsonl`이 없으면 받는다. 23MB다.

```bash
curl -fL -o personas.jsonl https://raw.githubusercontent.com/junleefc/first-7-customers/main/personas.jsonl \
  || (curl -fL -o personas.jsonl.gz https://cdn.jsdelivr.net/gh/junleefc/first-7-customers@main/personas.jsonl.gz && gunzip -f personas.jsonl.gz)
```

받은 뒤 `wc -l personas.jsonl`이 10000인지 확인한다. 아니면 다시 받는다.

파일은 10,000명, 한 줄에 한 명, JSON이다. NVIDIA Nemotron-Personas-Korea 100만 명을 연령대·성별·권역 비율 그대로 100분의 1로 줄인 것이다. 필드:

`uuid, age, sex, province, district, occupation, education_level, marital_status, family_type, housing_type, persona, professional_persona, family_persona, skills_and_expertise, hobbies_and_interests, career_goals_and_ambitions`

이름은 `persona` 텍스트 첫머리에 "OOO 씨"로 나온다. 그 이름을 그대로 쓴다. 새 이름을 짓지 않는다.

출처: NVIDIA Nemotron-Personas-Korea (CC BY 4.0). 리포트 맨 아래에 이 한 줄을 넣는다.

---

## 3. 7명 고르기

기획서의 "누구"에서 조건을 뽑는다. 쓸 수 있는 축: 나이 범위, 성별, 직업 키워드, 지역, 가족 상황(family_type, marital_status), 자유 키워드(persona, professional_persona, family_persona 텍스트 검색).

데이터의 한계를 먼저 알고 조건을 세운다.
- **자녀 나이와 학년은 프로필에 거의 없다.** "초등 2학년 자녀"는 family_type에 "자녀"가 있고 부모 나이가 30~45세인 사람으로 대체한다. 학년은 모르는 채로 둔다.
- **배우자가 일하는지는 알 수 없다.** "맞벌이"는 "일하는 부모"로 읽는다. 아빠 페르소나도 뽑는다.
- **과거 사건 기록이 없다.** "언제 겪었나"는 물을 수 없다. 생활 패턴에서 그 불편이 생길 상황이 있는지를 본다.
- 자유 키워드는 넓게 여러 개를 OR로 건다. 예: 부모 타깃이면 `["아이", "자녀", "학교", "학부모", "워킹맘", "맞벌이", "등하교", "준비물"]`. 한두 단어로 걸면 후보가 거의 안 나온다.

- 타깃 5명: 조건에 맞는 사람. 후보를 python으로 걸러 30명 안팎을 뽑고, 그중 직업·지역·가족 상황이 서로 다르게 5명을 고른다.
- 근처 2명: 한 축만 일부러 벗어난 사람. 예를 들어 나이가 타깃보다 10살 위거나 아래, 또는 직업이 다르지만 같은 불편을 겪을 법한 사람. 이 2명은 "나 말고 이 불편을 겪는 사람이 있나"를 확인하는 용도다.

조건 매칭이 20명 이상 나와야 한다. 5명 미만이면 조건을 하나씩 푼다. 푸는 순서: 지역 → 직업 키워드 → 자유 키워드 넓히기 → 나이 범위 넓히기. 어떤 조건을 어떻게 풀었는지 "선별 메모" 두 줄로 남긴다. 이 메모는 채팅과 REPORT.md 둘 다에 들어간다.

python 예시 (그대로 쓰지 말고 조건에 맞게 바꾼다):

```python
import json, random
rows = [json.loads(l) for l in open("personas.jsonl", encoding="utf-8")]
def hit(r):
    text = r["persona"] + r["professional_persona"] + r["family_persona"]
    return 30 <= r["age"] <= 45 \
        and "자녀" in r["family_type"] \
        and any(k in text for k in ["아이", "자녀", "학교", "학부모", "워킹맘", "맞벌이", "등하교", "준비물"])
cand = [r for r in rows if hit(r)]
random.seed()
random.shuffle(cand)
print("10,000명 중 조건 매칭", len(cand), "명")
for r in cand[:30]:
    print(r["age"], r["sex"], r["district"], r["occupation"], "|", r["persona"][:60])
```

선별 메모에 "10,000명 중 조건 매칭 N명"을 적는다.

고른 7명을 아래 표로 사용자에게 보여준 뒤 인터뷰로 넘어간다. 사용자 승인을 기다리지 않는다.

| 번호 | 이름 | 나이·성별 | 지역 | 직업 | 가족 | 타깃/근처 | 고른 이유 한 줄 |

---

## 4. 인터뷰 (7명 각각)

각 사람의 persona, professional_persona, family_persona, skills, hobbies, career_goals를 전부 읽고 그 사람이 되어 1인칭으로 답한다. 말투는 그 사람의 나이와 직업에 맞게. 답은 짧게, 각 질문에 2~3문장.

**Q1 불편.** 기획서의 불편이 생길 상황이 당신 일상에 있나. 있다면 어떤 장면인가. 없다면 왜 없나.

**Q2 지금 방법.** 그 불편을 지금은 어떻게 넘기고 있나. 그게 얼마나 귀찮은가. (프로필로 알 수 없으면 그렇게 쓴다.)

**Q3 차이.** 기획서의 "내 것은 이게 다르다"가 당신에게 의미 있나. 아니면 지금 방법이랑 별 차이 없나.

**Q4 가격.** 기획서의 가격을 낼 것인가. 이미 돈 내고 쓰는 비슷한 게 있나.

**Q5 페이지.** index.html 텍스트를 위에서 아래로 읽었다고 치고:
- 신청 버튼을 누르나: **누른다 / 안 누른다 / 모르겠다** 중 하나만.
- 안 누르거나 모르겠으면, 어느 구간에서 멈췄나: **첫 화면 / 차이 / 무료 제안 / 신청 항목** 중 하나만. (신청 항목이 걸림돌이면 어떤 항목이 부담인지도.) 누른 사람은 멈춘 곳이 **없음**이다.
- 뭐라고 바뀌면 누르나: 본인 말투로 한 문장. 누른다고 한 사람도 "이건 좀 걸렸다" 한 가지를 쓴다.

주의: Q1에서 불편이 없다고 한 사람은 Q5에서 "안 누른다"가 자연스럽다. 억지로 누르게 하지 않는다.

---

## 5. 종합

7명 결과를 모아 아래 순서로 정리한다. 이 순서가 리포트 상단이다.

1. **결과 한 줄.** "7명 중 N명이 누른다. M명은 안 누르고, K명은 모르겠다."
2. **가장 많이 걸린 구간 1개.** 안 누른/모르겠다 사람들이 멈춘 구간 중 최빈값. 단, **Q1에서 불편 자체가 없다고 한 사람은 계산에서 뺀다.** 그 사람은 문장을 고쳐도 안 누르는 사람이라 타깃 판단(5번)에만 쓴다. 계산에 남은 사람이 없으면 "누른다"고 한 사람들의 "걸린 것"에서 최빈값을 잡는다. 동률이면 페이지에서 더 위에 있는 구간.
3. **바꿀 문장 1개.** 그 구간에서 index.html에 실제로 있는 문장 하나를 인용하고(현재), 인터뷰 답을 근거로 바꾼 문장(제안)을 쓴다. 문장 하나는 태그 하나(h1, h2, p 중 하나)의 텍스트 전체다. 제안 문장은 기획서에 있는 말로 쓰고, 없는 약속을 지어내지 않는다.
4. **신청 항목.** 신청 항목이 걸림돌로 나온 사람이 있으면 "폼 질문을 O개로 줄이거나 OO 항목을 빼라"고 쓴다. 없으면 이 줄은 생략.
5. **근처 2명이 말해 주는 것.** 타깃 밖 2명의 반응으로 "타깃을 넓혀도 되나 / 좁혀야 하나" 한 줄.
6. **이건 가설이다.** "가상 고객 7명의 답은 통계 프로필로 만든 추정이다. 실제 사람 3명에게 아래 세 질문을 물어보고 확인하라." 그리고 실제 인터뷰 질문 3개. 질문은 이번 결과에서 가장 불확실했던 것을 겨냥한다.

---

## 6. 출력

두 파일을 현재 폴더에 저장한다.

**REPORT.md** : 이 순서로 쓴다.
1. 제목, 날짜, 읽은 파일 이름
2. 기획서에서 뽑은 여섯 가지 표 (1절)
3. 페이지 구간 (1절에서 뽑은 index.html 텍스트, 구간별로)
4. 종합 (5절의 여섯 항목)
5. 7명 표 + 선별 메모 (3절)
6. 7명 인터뷰 전문 (4절)
7. 출처 한 줄

사용자가 다음에 "REPORT.md를 반영해서 고쳐줘"라고 할 때 클로드가 다시 읽는 파일이다. 종합 3번의 "현재 문장"은 index.html에 있는 그대로 정확히 인용한다.

**REPORT.html** : 아래 템플릿을 받아 채운다. 템플릿 안의 `{{ }}` 자리를 전부 채우고, 카드는 7개를 만든다. 템플릿의 CSS와 구조는 바꾸지 않는다.

```bash
curl -fL -o report-template.html https://raw.githubusercontent.com/junleefc/first-7-customers/main/templates/report.html \
  || curl -fL -o report-template.html https://cdn.jsdelivr.net/gh/junleefc/first-7-customers@main/templates/report.html
```

템플릿의 치환 규칙은 템플릿 파일 맨 위 주석에 있다. 카드 헤더의 지역은 district 값의 하이픈을 공백으로 바꿔 쓴다. REPORT.html을 저장한 뒤 report-template.html은 지운다. 손으로 치환하지 말고 python으로 한다. 답변 텍스트는 `html.escape`로 넣는다(큰따옴표, `<`, `&`가 카드를 깨뜨린다).

저장이 끝나면 REPORT.html을 브라우저로 연다(macOS `open REPORT.html`, Windows `start REPORT.html`). 그리고 사용자에게 이렇게 마무리한다.

> 7명 중 N명이 누릅니다. 가장 많이 걸린 곳은 [구간]입니다.
> 고치려면 이렇게 말하세요: "REPORT.md의 결론을 반영해서 index.html을 고쳐줘."
